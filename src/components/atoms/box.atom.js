import styled from "styled-components/native";
import {
  space,
  color,
  layout,
  flexbox,
  position,
  typography,
  border,
  shadow,
  background,
} from "styled-system";

const Box = styled.View`
  ${space}
  ${color}
  ${layout}
  ${flexbox}
  ${position}
  ${typography}
  ${border}
  ${shadow}
  ${background}
`;

export default Box;